apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: three-tier-app
  labels:
    app: backend
data:
  PORT: "5000"
  NODE_ENV: "production"
  DB_HOST: "database-2.cvkyc0o8621q.ap-south-1.rds.amazonaws.com" # Replace with your AWS RDS Endpoint
  DB_PORT: "5432"
  DB_NAME: "postgres"
